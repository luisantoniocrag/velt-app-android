<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.App" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/velt_bg</item>
        <item name="android:statusBarColor">@color/velt_bg</item>
        <item name="android:navigationBarColor">@color/velt_bg</item>

        <!-- Splash del sistema (Android 12+): fondo oscuro de la app + palma cian -->
        <item name="android:windowSplashScreenBackground">@color/velt_bg</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
    </style>
</resources>
