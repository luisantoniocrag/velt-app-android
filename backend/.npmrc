# permissionless 0.2.x declara un peerOptional ox ^0.8 que choca con el ox 0.14
# que trae viem 2.5x. Es peerOptional (permissionless usa viem por debajo, no ox
# directo), así que se ignora con seguridad. Sin esto, `npm install <pkg>` falla.
legacy-peer-deps=true
